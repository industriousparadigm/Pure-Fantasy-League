<h1>
  <div class="pull-right"><%= link_to 'New League', new_resource_path, class: 'btn btn-primary' %></div>
  Leagues
</h1>

<% if collection.empty? %>
  <h4 class="alert alert-warning">There are no Leagues yet! <%= link_to 'Create one now', new_resource_path %></h4>
<% else %>
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Title</th>
        <th width="120"></th>
      </tr>
    </thead>

    <tbody>
      <% collection.each do |league| %>
        <tr>
          <td><strong><%= link_to league, resource_path(league) %></strong></td>
          <td align="right">
            <%= link_to 'Edit', edit_resource_path(league) %> |
            <%= link_to 'Destroy', resource_path(league), method: :delete, data: { confirm: 'Are you sure?' } %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
